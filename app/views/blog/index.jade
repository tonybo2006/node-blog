extends ../layout/layout

block content
  .row
    .col-lg-2
      .sidebar-module
        .sidebar-module-inset
          h4 User
          p sekjtewijtewt
      .sidebar-module
        h4 分类
        ul
          //-each blog, index in blogs
          //  li
          //    a(href='/blogs/types/'+type)=blog.type

    .col-lg-10
      a(href='/blogs/new') 发表Blog
      ol
        -each blog, index in blogs
          li.list-unstyled
            a(href='/blogs/'+blog.id) #{blog.title}
    nav
      ul.pager
        li
          a(href='#') Previous
          | &nbsp;
          a(href='#') Next